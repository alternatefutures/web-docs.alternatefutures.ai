---
title: "Mutations"
description: "Every mutation the Alternate Clouds GraphQL API exposes, with arguments and return types, generated from the API source."
---

{/* AUTO-GENERATED by scripts/generate-graphql-docs.mjs from @alternatefutures/backend@0.1.2. Do not edit by hand. */}

<Callout type="info">
Generated from the API server source (@alternatefutures/backend 0.1.2) on every merge. Endpoint
`https://api.alternatefutures.ai/graphql`, header `Authorization: Bearer <personal access token>`.
See the [GraphQL API overview](/api) for how to call it. Provider-specific types, fields and arguments are omitted here; introspection on the endpoint returns the complete live schema.
</Callout>

## Auth - SDK token exchange

### loginWithPersonalAccessToken

Exchange a Personal Access Token for a short-lived access token.
Used by the SDK to authenticate subsequent requests.

| Argument | Type | Description |
|----------|------|-------------|
| `data` | `LoginWithPersonalAccessTokenDataInput!` · [LoginWithPersonalAccessTokenDataInput](/api/inputs#loginwithpersonalaccesstokendatainput) |  |

Returns `String!`.

## Auth

### createPersonalAccessToken

| Argument | Type | Description |
|----------|------|-------------|
| `name` | `String!` |  |
| `expiresAt` | `Date` |  |

Returns `PersonalAccessTokenCreated!` · [PersonalAccessTokenCreated](/api/objects#personalaccesstokencreated).

### deletePersonalAccessToken

| Argument | Type | Description |
|----------|------|-------------|
| `id` | `ID!` |  |

Returns `Boolean!`.

## Projects

### createProject

| Argument | Type | Description |
|----------|------|-------------|
| `data` | `CreateProjectDataInput!` · [CreateProjectDataInput](/api/inputs#createprojectdatainput) |  |

Returns `Project!` · [Project](/api/objects#project).

### updateProject

| Argument | Type | Description |
|----------|------|-------------|
| `id` | `ID!` |  |
| `data` | `UpdateProjectDataInput!` · [UpdateProjectDataInput](/api/inputs#updateprojectdatainput) |  |

Returns `Project!` · [Project](/api/objects#project).

### deleteProject

| Argument | Type | Description |
|----------|------|-------------|
| `id` | `ID!` |  |

Returns `Boolean!`.

### projectChatSession

Mint a single-use join ticket (plus room id, relay URL and room secret)
for a project's chat. Requires project access; organization projects
only. Relay-blind, not end-to-end: the platform can read project chat.

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |

Returns `ProjectChatSession!` · [ProjectChatSession](/api/objects#projectchatsession).

### publicChatTicket

Mint a single-use join ticket for the hosted public chat. Requires only an
authenticated caller: no org or project check. The ticket is NOT bound to
a room: the platform never learns which public rooms exist or who is in
them, only that this user opened chat.

Returns `PublicChatTicket!` · [PublicChatTicket](/api/objects#publicchatticket).

## Services

### createService

| Argument | Type | Description |
|----------|------|-------------|
| `input` | `CreateServiceInput!` · [CreateServiceInput](/api/inputs#createserviceinput) |  |

Returns `Service!` · [Service](/api/objects#service).

### updateService

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |
| `input` | `UpdateServiceInput!` · [UpdateServiceInput](/api/inputs#updateserviceinput) |  |

Returns `Service!` · [Service](/api/objects#service).

### updateServicePriority

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |
| `shutdownPriority` | `Int!` |  |

Returns `Service!` · [Service](/api/objects#service).

## Attestation registry (org allowlist; af hosts, org authors)

### addAttestationMeasurement

Bless a build measurement (compose_hash) in the active org's allowlist.

| Argument | Type | Description |
|----------|------|-------------|
| `input` | `AddAttestationMeasurementInput!` · [AddAttestationMeasurementInput](/api/inputs#addattestationmeasurementinput) |  |

Returns `AttestationMeasurement!` · [AttestationMeasurement](/api/objects#attestationmeasurement).

### revokeAttestationMeasurement

Revoke a blessed measurement. Idempotent; revocation retires every identity minted from it.

| Argument | Type | Description |
|----------|------|-------------|
| `id` | `ID!` |  |

Returns `AttestationMeasurement!` · [AttestationMeasurement](/api/objects#attestationmeasurement).

### registerAttestedIdentity

Verify a server's quote (Intel DCAP roots, in-process) and record its
identity. Requires the quote's measurement to be ACTIVE in the active
org's allowlist and the quote's report_data to bind the pubkey.

| Argument | Type | Description |
|----------|------|-------------|
| `input` | `RegisterAttestedIdentityInput!` · [RegisterAttestedIdentityInput](/api/inputs#registerattestedidentityinput) |  |

Returns `AttestedIdentity!` · [AttestedIdentity](/api/objects#attestedidentity).

## Sites

### createSite

| Argument | Type | Description |
|----------|------|-------------|
| `data` | `SiteDataInput!` · [SiteDataInput](/api/inputs#sitedatainput) |  |

Returns `Site!` · [Site](/api/objects#site).

### deleteSite

| Argument | Type | Description |
|----------|------|-------------|
| `where` | `SiteWhereInput!` · [SiteWhereInput](/api/inputs#sitewhereinput) |  |

Returns `Site!` · [Site](/api/objects#site).

## Deployments

### createCustomIpfsDeployment

| Argument | Type | Description |
|----------|------|-------------|
| `data` | `DeploymentDataInput!` · [DeploymentDataInput](/api/inputs#deploymentdatainput) |  |

Returns `SiteDeployment!` · [SiteDeployment](/api/objects#sitedeployment).

### createDeployment

| Argument | Type | Description |
|----------|------|-------------|
| `siteId` | `ID!` |  |
| `sourceDirectory` | `String!` |  |
| `storageType` | `StorageType` · [StorageType](/api/enums#storagetype) |  |
| `buildOptions` | `BuildOptionsInput` · [BuildOptionsInput](/api/inputs#buildoptionsinput) |  |

Returns `SiteDeployment!` · [SiteDeployment](/api/objects#sitedeployment).

## Functions

### createAFFunction

| Argument | Type | Description |
|----------|------|-------------|
| `data` | `CreateAFFunctionDataInput!` · [CreateAFFunctionDataInput](/api/inputs#createaffunctiondatainput) |  |

Returns `AFFunction!` · [AFFunction](/api/objects#affunction).

### updateAFFunction

| Argument | Type | Description |
|----------|------|-------------|
| `where` | `AFFunctionWhereInput!` · [AFFunctionWhereInput](/api/inputs#affunctionwhereinput) |  |
| `data` | `UpdateAFFunctionDataInput!` · [UpdateAFFunctionDataInput](/api/inputs#updateaffunctiondatainput) |  |

Returns `AFFunction!` · [AFFunction](/api/objects#affunction).

### triggerAFFunctionDeployment

| Argument | Type | Description |
|----------|------|-------------|
| `where` | `TriggerAFFunctionDeploymentWhereInput!` · [TriggerAFFunctionDeploymentWhereInput](/api/inputs#triggeraffunctiondeploymentwhereinput) |  |
| `data` | `TriggerAFFunctionDeploymentDataInput` · [TriggerAFFunctionDeploymentDataInput](/api/inputs#triggeraffunctiondeploymentdatainput) |  |

Returns `AFFunctionDeployment!` · [AFFunctionDeployment](/api/objects#affunctiondeployment).

### deleteAFFunction

| Argument | Type | Description |
|----------|------|-------------|
| `where` | `DeleteAFFunctionWhereInput!` · [DeleteAFFunctionWhereInput](/api/inputs#deleteaffunctionwhereinput) |  |

Returns `AFFunction!` · [AFFunction](/api/objects#affunction).

### deleteService

| Argument | Type | Description |
|----------|------|-------------|
| `id` | `ID!` |  |

Returns `Service!` · [Service](/api/objects#service).

## Domains

### createDomain

| Argument | Type | Description |
|----------|------|-------------|
| `input` | `CreateDomainInput!` · [CreateDomainInput](/api/inputs#createdomaininput) |  |

Returns `Domain!` · [Domain](/api/objects#domain).

### createOrgDomain

| Argument | Type | Description |
|----------|------|-------------|
| `input` | `CreateOrgDomainInput!` · [CreateOrgDomainInput](/api/inputs#createorgdomaininput) |  |

Returns `Domain!` · [Domain](/api/objects#domain).

### assignDomainToSite

| Argument | Type | Description |
|----------|------|-------------|
| `domainId` | `ID!` |  |
| `siteId` | `ID!` |  |

Returns `Domain!` · [Domain](/api/objects#domain).

### verifyDomain

| Argument | Type | Description |
|----------|------|-------------|
| `domainId` | `ID!` |  |

Returns `Boolean!`.

### provisionSsl

| Argument | Type | Description |
|----------|------|-------------|
| `domainId` | `ID!` |  |
| `email` | `String!` |  |

Returns `Domain!` · [Domain](/api/objects#domain).

### renewSslCertificate

| Argument | Type | Description |
|----------|------|-------------|
| `domainId` | `ID!` |  |

Returns `Domain!` · [Domain](/api/objects#domain).

### setPrimaryDomain

| Argument | Type | Description |
|----------|------|-------------|
| `siteId` | `ID!` |  |
| `domainId` | `ID!` |  |

Returns `Boolean!`.

### deleteDomain

| Argument | Type | Description |
|----------|------|-------------|
| `id` | `ID!` |  |

Returns `Boolean!`.

## Agent chat

### createAgent

| Argument | Type | Description |
|----------|------|-------------|
| `input` | `CreateAgentInput!` · [CreateAgentInput](/api/inputs#createagentinput) |  |

Returns `Agent!` · [Agent](/api/objects#agent).

### createChat

| Argument | Type | Description |
|----------|------|-------------|
| `input` | `CreateChatInput!` · [CreateChatInput](/api/inputs#createchatinput) |  |

Returns `Chat!` · [Chat](/api/objects#chat).

### sendMessage

| Argument | Type | Description |
|----------|------|-------------|
| `input` | `SendMessageInput!` · [SendMessageInput](/api/inputs#sendmessageinput) |  |

Returns `Message!` · [Message](/api/objects#message).

### deleteChat

| Argument | Type | Description |
|----------|------|-------------|
| `id` | `ID!` |  |

Returns `Boolean!`.

## Storage tracking

### triggerStorageSnapshot

Returns `StorageSnapshot!` · [StorageSnapshot](/api/objects#storagesnapshot).

## Observability settings

### updateObservabilitySettings

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `input` | `UpdateObservabilitySettingsInput!` · [UpdateObservabilitySettingsInput](/api/inputs#updateobservabilitysettingsinput) |  |

Returns `ObservabilitySettings!` · [ObservabilitySettings](/api/objects#observabilitysettings).

## Domain registration / purchase

### purchaseDomain

| Argument | Type | Description |
|----------|------|-------------|
| `input` | `PurchaseDomainInput!` · [PurchaseDomainInput](/api/inputs#purchasedomaininput) |  |

Returns `DomainRegistrationResult!` · [DomainRegistrationResult](/api/objects#domainregistrationresult).

## DNS record management (admin)

### addDnsRecord

| Argument | Type | Description |
|----------|------|-------------|
| `input` | `CreateDNSRecordInput!` · [CreateDNSRecordInput](/api/inputs#creatednsrecordinput) |  |

Returns `DNSUpdateResult!` · [DNSUpdateResult](/api/objects#dnsupdateresult).

### updateDnsRecord

| Argument | Type | Description |
|----------|------|-------------|
| `input` | `UpdateDNSRecordInput!` · [UpdateDNSRecordInput](/api/inputs#updatednsrecordinput) |  |

Returns `DNSUpdateResult!` · [DNSUpdateResult](/api/objects#dnsupdateresult).

### deleteDnsRecord

| Argument | Type | Description |
|----------|------|-------------|
| `input` | `DeleteDNSRecordInput!` · [DeleteDNSRecordInput](/api/inputs#deletednsrecordinput) |  |

Returns `DNSUpdateResult!` · [DNSUpdateResult](/api/objects#dnsupdateresult).

## Web3 domains

### registerArns

| Argument | Type | Description |
|----------|------|-------------|
| `domainId` | `ID!` |  |
| `arnsName` | `String!` |  |
| `contentId` | `String!` |  |

Returns `Domain!` · [Domain](/api/objects#domain).

### updateArnsContent

| Argument | Type | Description |
|----------|------|-------------|
| `domainId` | `ID!` |  |
| `contentId` | `String!` |  |

Returns `Domain!` · [Domain](/api/objects#domain).

### setEnsContentHash

| Argument | Type | Description |
|----------|------|-------------|
| `domainId` | `ID!` |  |
| `ensName` | `String!` |  |
| `contentHash` | `String!` |  |

Returns `Domain!` · [Domain](/api/objects#domain).

### publishIpns

| Argument | Type | Description |
|----------|------|-------------|
| `domainId` | `ID!` |  |
| `cid` | `String!` |  |

Returns `Domain!` · [Domain](/api/objects#domain).

### updateIpns

| Argument | Type | Description |
|----------|------|-------------|
| `domainId` | `ID!` |  |
| `cid` | `String!` |  |

Returns `Domain!` · [Domain](/api/objects#domain).

## Templates

### deployCompositeTemplate

| Argument | Type | Description |
|----------|------|-------------|
| `input` | `DeployCompositeTemplateInput!` · [DeployCompositeTemplateInput](/api/inputs#deploycompositetemplateinput) |  |

Returns `CompositeDeploymentResult!` · [CompositeDeploymentResult](/api/objects#compositedeploymentresult).

## Service environment variables

### setServiceEnvVar

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |
| `key` | `String!` |  |
| `value` | `String!` |  |
| `secret` | `Boolean` |  |

Returns `ServiceEnvVar!` · [ServiceEnvVar](/api/objects#serviceenvvar).

### deleteServiceEnvVar

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |
| `key` | `String!` |  |

Returns `Boolean!`.

### bulkSetServiceEnvVars

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |
| `vars` | `[EnvVarInput!]!` · [EnvVarInput](/api/inputs#envvarinput) |  |

Returns `[ServiceEnvVar!]!` · [ServiceEnvVar](/api/objects#serviceenvvar).

## Back via cache or polling, the client should fetch on-demand only.

### revealServiceEnvVar

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |
| `key` | `String!` |  |

Returns `String!`.

## Service port configuration

### setServicePort

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |
| `containerPort` | `Int!` |  |
| `publicPort` | `Int` |  |
| `protocol` | `String` |  |

Returns `ServicePort!` · [ServicePort](/api/objects#serviceport).

### deleteServicePort

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |
| `containerPort` | `Int!` |  |

Returns `Boolean!`.

## Service linking

### linkServices

| Argument | Type | Description |
|----------|------|-------------|
| `sourceServiceId` | `ID!` |  |
| `targetServiceId` | `ID!` |  |
| `alias` | `String` |  |

Returns `ServiceLink!` · [ServiceLink](/api/objects#servicelink).

### unlinkServices

| Argument | Type | Description |
|----------|------|-------------|
| `sourceServiceId` | `ID!` |  |
| `targetServiceId` | `ID!` |  |

Returns `Boolean!`.

## Capped at 1 mb and target the running container (ephemeral).

### writeServiceFile

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |
| `path` | `String!` |  |
| `content` | `String!` |  |

Returns `ServiceFileWriteResult!` · [ServiceFileWriteResult](/api/objects#servicefilewriteresult).

### createServiceDirectory

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |
| `path` | `String!` |  |

Returns `Boolean!`.

### renameServiceFile

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |
| `fromPath` | `String!` |  |
| `toPath` | `String!` |  |

Returns `Boolean!`.

### deleteServiceFile

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |
| `path` | `String!` |  |

Returns `Boolean!`.

## Feedback

### submitFeedback

| Argument | Type | Description |
|----------|------|-------------|
| `input` | `SubmitFeedbackInput!` · [SubmitFeedbackInput](/api/inputs#submitfeedbackinput) |  |

Returns `FeedbackReport!` · [FeedbackReport](/api/objects#feedbackreport).

## Other

### syncGithubInstallation

Create a canonical GitHub install row or grant this AF org access to an existing install after GitHub redirects with installation_id.

| Argument | Type | Description |
|----------|------|-------------|
| `installationId` | `String!` |  |
| `orgId` | `ID` |  |

Returns `GithubInstallation!` · [GithubInstallation](/api/objects#githubinstallation).

### refreshGithubInstallations

Live-pull all accessible App installations from GitHub into the local DB and return them.
Slow path: UI uses this only on explicit Refresh / post-install.

| Argument | Type | Description |
|----------|------|-------------|
| `orgId` | `ID` |  |

Returns `[GithubInstallation!]!` · [GithubInstallation](/api/objects#githubinstallation).

### uninstallGithubInstallation

Uninstall the AlternateFutures GitHub App from the account/org backing this installation.
Calls DELETE /app/installations/\{id} on GitHub with the App JWT, then cleans up the local row
and nulls gitInstallationId on linked services. Caller must be a member of an AF org with
access to the installation. Returns true on success.

| Argument | Type | Description |
|----------|------|-------------|
| `installationId` | `ID!` |  |

Returns `Boolean!`.

### createGithubService

| Argument | Type | Description |
|----------|------|-------------|
| `input` | `CreateGithubServiceInput!` · [CreateGithubServiceInput](/api/inputs#creategithubserviceinput) |  |

Returns `Service!` · [Service](/api/objects#service).

### connectGithubRepo

| Argument | Type | Description |
|----------|------|-------------|
| `input` | `ConnectGithubRepoInput!` · [ConnectGithubRepoInput](/api/inputs#connectgithubrepoinput) |  |

Returns `Service!` · [Service](/api/objects#service).

### redeployGithubService

Trigger a new build. With no sha, builds the tip of the tracked branch (default behaviour, used by top-level Rebuild). With a sha (7-40 char hex commit SHA), rebuilds that exact commit: used by the per-row rebuild button in build history.

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |
| `sha` | `String` |  |

Returns `BuildJob!` · [BuildJob](/api/objects#buildjob).

