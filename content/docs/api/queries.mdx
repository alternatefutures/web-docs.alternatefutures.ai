---
title: "Queries"
description: "Every query the Alternate Clouds GraphQL API exposes, with arguments and return types, generated from the API source."
---

{/* AUTO-GENERATED by scripts/generate-graphql-docs.mjs from @alternatefutures/backend@0.1.2. Do not edit by hand. */}

<Callout type="info">
Generated from the API server source (@alternatefutures/backend 0.1.2) on every merge. Endpoint
`https://api.alternatefutures.ai/graphql`, header `Authorization: Bearer <personal access token>`.
See the [GraphQL API overview](/api) for how to call it. Provider-specific types, fields and arguments are omitted here; introspection on the endpoint returns the complete live schema.
</Callout>

## Version

### version

Returns `Version!` · [Version](/api/objects#version).

## Templates

### templates

| Argument | Type | Description |
|----------|------|-------------|
| `category` | `TemplateCategory` · [TemplateCategory](/api/enums#templatecategory) |  |

Returns `[Template!]!` · [Template](/api/objects#template).

### template

| Argument | Type | Description |
|----------|------|-------------|
| `id` | `ID!` |  |

Returns `Template` · [Template](/api/objects#template).

## User & auth

### me

Returns `User` · [User](/api/objects#user).

### personalAccessTokens

Returns `PersonalAccessTokenList!` · [PersonalAccessTokenList](/api/objects#personalaccesstokenlist).

### apiKeyRateLimit

Returns `ApiKeyRateLimit!` · [ApiKeyRateLimit](/api/objects#apikeyratelimit).

## Projects

### project

| Argument | Type | Description |
|----------|------|-------------|
| `id` | `ID!` |  |

Returns `Project` · [Project](/api/objects#project).

### projects

Returns `ProjectList!` · [ProjectList](/api/objects#projectlist).

## Service registry

### serviceRegistry

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID` |  |

Returns `[Service!]!` · [Service](/api/objects#service).

## Sites

### site

| Argument | Type | Description |
|----------|------|-------------|
| `where` | `SiteWhereInput!` · [SiteWhereInput](/api/inputs#sitewhereinput) |  |

Returns `Site` · [Site](/api/objects#site).

### sites

| Argument | Type | Description |
|----------|------|-------------|
| `where` | `SitesWhereInput` · [SitesWhereInput](/api/inputs#siteswhereinput) |  |

Returns `SiteList!` · [SiteList](/api/objects#sitelist).

### siteBySlug

| Argument | Type | Description |
|----------|------|-------------|
| `where` | `SiteBySlugWhereInput!` · [SiteBySlugWhereInput](/api/inputs#sitebyslugwhereinput) |  |

Returns `Site` · [Site](/api/objects#site).

## IPNS records

### ipnsRecord

| Argument | Type | Description |
|----------|------|-------------|
| `name` | `String!` |  |

Returns `IPNSRecord` · [IPNSRecord](/api/objects#ipnsrecord).

### ipnsRecords

Returns `IpnsRecordList!` · [IpnsRecordList](/api/objects#ipnsrecordlist).

## Private gateways

### privateGateway

| Argument | Type | Description |
|----------|------|-------------|
| `id` | `ID!` |  |

Returns `PrivateGateway` · [PrivateGateway](/api/objects#privategateway).

### privateGatewayBySlug

| Argument | Type | Description |
|----------|------|-------------|
| `slug` | `String!` |  |

Returns `PrivateGateway` · [PrivateGateway](/api/objects#privategateway).

### privateGateways

Returns `PrivateGatewayList!` · [PrivateGatewayList](/api/objects#privategatewaylist).

## Deployments

### deployment

| Argument | Type | Description |
|----------|------|-------------|
| `where` | `DeploymentWhereInput!` · [DeploymentWhereInput](/api/inputs#deploymentwhereinput) |  |

Returns `SiteDeployment` · [SiteDeployment](/api/objects#sitedeployment).

### deployments

| Argument | Type | Description |
|----------|------|-------------|
| `siteId` | `ID` |  |

Returns `[SiteDeployment!]!` · [SiteDeployment](/api/objects#sitedeployment).

## Functions

### afFunction

| Argument | Type | Description |
|----------|------|-------------|
| `where` | `AFFunctionWhereInput!` · [AFFunctionWhereInput](/api/inputs#affunctionwhereinput) |  |

Returns `AFFunction` · [AFFunction](/api/objects#affunction).

### afFunctionByName

| Argument | Type | Description |
|----------|------|-------------|
| `where` | `AFFunctionByNameWhereInput!` · [AFFunctionByNameWhereInput](/api/inputs#affunctionbynamewhereinput) |  |

Returns `AFFunction` · [AFFunction](/api/objects#affunction).

### afFunctions

Returns `AFFunctionList!` · [AFFunctionList](/api/objects#affunctionlist).

### afFunctionDeployment

| Argument | Type | Description |
|----------|------|-------------|
| `where` | `AFFunctionDeploymentWhereInput!` · [AFFunctionDeploymentWhereInput](/api/inputs#affunctiondeploymentwhereinput) |  |

Returns `AFFunctionDeployment` · [AFFunctionDeployment](/api/objects#affunctiondeployment).

### afFunctionDeployments

| Argument | Type | Description |
|----------|------|-------------|
| `where` | `AFFunctionDeploymentsWhereInput!` · [AFFunctionDeploymentsWhereInput](/api/inputs#affunctiondeploymentswhereinput) |  |

Returns `AFFunctionDeploymentList!` · [AFFunctionDeploymentList](/api/objects#affunctiondeploymentlist).

## Domains

### domain

| Argument | Type | Description |
|----------|------|-------------|
| `id` | `ID!` |  |

Returns `Domain` · [Domain](/api/objects#domain).

### domains

Returns `DomainList!` · [DomainList](/api/objects#domainlist).

### domainByHostname

| Argument | Type | Description |
|----------|------|-------------|
| `hostname` | `String!` |  |

Returns `Domain` · [Domain](/api/objects#domain).

### domainVerificationInstructions

| Argument | Type | Description |
|----------|------|-------------|
| `domainId` | `ID!` |  |

Returns `DomainVerificationInstructions!` · [DomainVerificationInstructions](/api/objects#domainverificationinstructions).

### sslCertificateStatus

Returns `[SslCertificateStatusInfo!]!` · [SslCertificateStatusInfo](/api/objects#sslcertificatestatusinfo).

### orgDomains

| Argument | Type | Description |
|----------|------|-------------|
| `orgId` | `ID!` |  |

Returns `[Domain!]!` · [Domain](/api/objects#domain).

## Zones (SDK compatibility)

### zones

Returns `ZoneList!` · [ZoneList](/api/objects#zonelist).

### zone

| Argument | Type | Description |
|----------|------|-------------|
| `id` | `ID!` |  |

Returns `Zone` · [Zone](/api/objects#zone).

## Storage (SDK compatibility)

### pins

Returns `PinList!` · [PinList](/api/objects#pinlist).

### pin

| Argument | Type | Description |
|----------|------|-------------|
| `where` | `PinWhereInput!` · [PinWhereInput](/api/inputs#pinwhereinput) |  |

Returns `Pin` · [Pin](/api/objects#pin).

### pinsByFilename

| Argument | Type | Description |
|----------|------|-------------|
| `where` | `PinsByFilenameWhereInput!` · [PinsByFilenameWhereInput](/api/inputs#pinsbyfilenamewhereinput) |  |

Returns `PinList!` · [PinList](/api/objects#pinlist).

### filecoinDeals

| Argument | Type | Description |
|----------|------|-------------|
| `where` | `FilecoinDealsWhereInput!` · [FilecoinDealsWhereInput](/api/inputs#filecoindealswhereinput) |  |

Returns `FilecoinDealList!` · [FilecoinDealList](/api/objects#filecoindeallist).

## ENS (SDK compatibility - minimal)

### ensRecords

Returns `EnsRecordList!` · [EnsRecordList](/api/objects#ensrecordlist).

### ensRecordsByIpnsId

| Argument | Type | Description |
|----------|------|-------------|
| `where` | `IPNSRecordWhereInput!` · [IPNSRecordWhereInput](/api/inputs#ipnsrecordwhereinput) |  |

Returns `EnsRecordList!` · [EnsRecordList](/api/objects#ensrecordlist).

## Applications (SDK compatibility - minimal)

### applications

Returns `ApplicationList!` · [ApplicationList](/api/objects#applicationlist).

## Domain registration / purchase

### checkDomainAvailability

| Argument | Type | Description |
|----------|------|-------------|
| `input` | `CheckDomainAvailabilityInput!` · [CheckDomainAvailabilityInput](/api/inputs#checkdomainavailabilityinput) |  |

Returns `[DomainAvailability!]!` · [DomainAvailability](/api/objects#domainavailability).

### domainPricing

| Argument | Type | Description |
|----------|------|-------------|
| `name` | `String!` |  |
| `extension` | `String!` |  |
| `operation` | `String` |  |
| `period` | `Int` |  |

Returns `DomainPricing!` · [DomainPricing](/api/objects#domainpricing).

### registeredDomains

| Argument | Type | Description |
|----------|------|-------------|
| `limit` | `Int` |  |
| `offset` | `Int` |  |
| `status` | `String` |  |

Returns `RegisteredDomainList!` · [RegisteredDomainList](/api/objects#registereddomainlist).

## DNS record management (admin)

### dnsRecords

| Argument | Type | Description |
|----------|------|-------------|
| `domain` | `String!` |  |

Returns `[DNSRecord!]!` · [DNSRecord](/api/objects#dnsrecord).

### dnsRecord

| Argument | Type | Description |
|----------|------|-------------|
| `domain` | `String!` |  |
| `name` | `String!` |  |
| `type` | `DNSRecordType!` · [DNSRecordType](/api/enums#dnsrecordtype) |  |

Returns `DNSRecord` · [DNSRecord](/api/objects#dnsrecord).

## Storage analytics

### storageAnalytics

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID` |  |

Returns `StorageAnalytics!` · [StorageAnalytics](/api/objects#storageanalytics).

### storageUsageTrend

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID` |  |
| `days` | `Int` |  |

Returns `[StorageUsageTrend!]!` · [StorageUsageTrend](/api/objects#storageusagetrend).

## Workspace metrics (aggregated compute, storage, traffic)

### workspaceMetrics

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID` |  |

Returns `WorkspaceMetrics!` · [WorkspaceMetrics](/api/objects#workspacemetrics).

## Unified deployments (all deployment types across all services)

### allDeployments

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID` |  |
| `limit` | `Int` |  |

Returns `[UnifiedDeployment!]!` · [UnifiedDeployment](/api/objects#unifieddeployment).

## System health

### subscriptionHealth

Returns `SubscriptionHealth!` · [SubscriptionHealth](/api/objects#subscriptionhealth).

## Agent chat

### agent

| Argument | Type | Description |
|----------|------|-------------|
| `id` | `ID!` |  |

Returns `Agent` · [Agent](/api/objects#agent).

### agentBySlug

| Argument | Type | Description |
|----------|------|-------------|
| `slug` | `String!` |  |

Returns `Agent` · [Agent](/api/objects#agent).

### agents

Returns `[Agent!]!` · [Agent](/api/objects#agent).

### chat

| Argument | Type | Description |
|----------|------|-------------|
| `id` | `ID!` |  |

Returns `Chat` · [Chat](/api/objects#chat).

### chats

Returns `[Chat!]!` · [Chat](/api/objects#chat).

### messages

| Argument | Type | Description |
|----------|------|-------------|
| `chatId` | `ID!` |  |
| `limit` | `Int` |  |
| `before` | `String` |  |

Returns `[Message!]!` · [Message](/api/objects#message).

## Storage tracking

### pinnedContent

| Argument | Type | Description |
|----------|------|-------------|
| `limit` | `Int` |  |

Returns `[PinnedContent!]!` · [PinnedContent](/api/objects#pinnedcontent).

### storageSnapshots

| Argument | Type | Description |
|----------|------|-------------|
| `startDate` | `Date` |  |
| `endDate` | `Date` |  |
| `limit` | `Int` |  |

Returns `[StorageSnapshot!]!` · [StorageSnapshot](/api/objects#storagesnapshot).

### storageStats

Returns `StorageTrackingStats!` · [StorageTrackingStats](/api/objects#storagetrackingstats).

## Observability / APM

### traces

| Argument | Type | Description |
|----------|------|-------------|
| `input` | `TraceQueryInput!` · [TraceQueryInput](/api/inputs#tracequeryinput) |  |

Returns `[Trace!]!` · [Trace](/api/objects#trace).

### trace

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `traceId` | `String!` |  |

Returns `Trace` · [Trace](/api/objects#trace).

### metrics

| Argument | Type | Description |
|----------|------|-------------|
| `input` | `MetricQueryInput!` · [MetricQueryInput](/api/inputs#metricqueryinput) |  |

Returns `[MetricSeries!]!` · [MetricSeries](/api/objects#metricseries).

### logs

| Argument | Type | Description |
|----------|------|-------------|
| `input` | `LogQueryInput!` · [LogQueryInput](/api/inputs#logqueryinput) |  |

Returns `[LogEntry!]!` · [LogEntry](/api/objects#logentry).

### services

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `startTime` | `Date!` |  |
| `endTime` | `Date!` |  |

Returns `[ServiceStats!]!` · [ServiceStats](/api/objects#servicestats).

### observabilitySettings

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |

Returns `ObservabilitySettings` · [ObservabilitySettings](/api/objects#observabilitysettings).

### telemetryUsage

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `startDate` | `Date!` |  |
| `endDate` | `Date!` |  |

Returns `TelemetryUsageSummary!` · [TelemetryUsageSummary](/api/objects#telemetryusagesummary).

## Live container health from provider

### deploymentHealth

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |

Returns `DeploymentHealth` · [DeploymentHealth](/api/objects#deploymenthealth).

## Service container logs

### serviceLogs

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |
| `tail` | `Int` |  |
| `service` | `String` |  |

Returns `ServiceLogResult!` · [ServiceLogResult](/api/objects#servicelogresult).

### attestationMeasurements

The active organization's attestation measurement allowlist.

Returns `[AttestationMeasurement!]!` · [AttestationMeasurement](/api/objects#attestationmeasurement).

### serviceAttestedIdentities

Registry identities linked to this service, newest first. status is
the EFFECTIVE status (a revoked measurement retires its identities): the same verdict the public REST lookup serves.

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |

Returns `[AttestedIdentity!]!` · [AttestedIdentity](/api/objects#attestedidentity).

## Deployment file access (files tab): container filesystem, ephemeral

### serviceFiles

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |
| `path` | `String!` |  |

Returns `ServiceFileList!` · [ServiceFileList](/api/objects#servicefilelist).

### serviceFileContent

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |
| `path` | `String!` |  |

Returns `ServiceFileContent!` · [ServiceFileContent](/api/objects#servicefilecontent).

### serviceFileAccessPolicy

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |

Returns `ServiceFileAccessPolicy!` · [ServiceFileAccessPolicy](/api/objects#servicefileaccesspolicy).

## Service links (connections between services)

### serviceLinks

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |

Returns `[ServiceLink!]!` · [ServiceLink](/api/objects#servicelink).

## Org billing runway (how long until funds run out)

### orgBillingRunway

Returns `OrgBillingRunway` · [OrgBillingRunway](/api/objects#orgbillingrunway).

## Other

### githubAppEnabled

Returns `Boolean!`.

### githubAppSlug

Returns `String`.

### githubInstallations

| Argument | Type | Description |
|----------|------|-------------|
| `orgId` | `ID` |  |

Returns `[GithubInstallation!]!` · [GithubInstallation](/api/objects#githubinstallation).

### githubInstallationRepos

| Argument | Type | Description |
|----------|------|-------------|
| `installationId` | `ID!` |  |

Returns `[GithubRepoSummary!]!` · [GithubRepoSummary](/api/objects#githubreposummary).

### githubRepoBranches

| Argument | Type | Description |
|----------|------|-------------|
| `installationId` | `ID!` |  |
| `owner` | `String!` |  |
| `repo` | `String!` |  |

Returns `[GithubBranchSummary!]!` · [GithubBranchSummary](/api/objects#githubbranchsummary).

### buildJob

Fetch a single BuildJob by id (auth: caller must have access to the parent service's project). Used by the Source-tab build-history UI to lazy-load the logs blob on row expand.

| Argument | Type | Description |
|----------|------|-------------|
| `id` | `ID!` |  |

Returns `BuildJob` · [BuildJob](/api/objects#buildjob).

### serviceBuildJobs

List recent BuildJobs for a service, newest first. Logs blob omitted: fetch it via buildJob(id) on demand. Used by the Source-tab build-history list (polled every 3s while a build is in flight).

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |
| `limit` | `Int` |  |

Returns `[BuildJob!]!` · [BuildJob](/api/objects#buildjob).

### regions

Return curated region buckets with live availability + pricing for the picker. Defaults to the standard compute tier when provider is omitted. The gpuModelHint argument is reserved for future per-model price emphasis;
ignored today.

| Argument | Type | Description |
|----------|------|-------------|
| `gpuModelHint` | `String` |  |

Some provider-specific arguments are omitted. †

Returns `[Region!]!` · [Region](/api/objects#region).

## Subscriptions

Subscriptions use the same endpoint over WebSocket (graphql-ws).

### deploymentLogs

| Argument | Type | Description |
|----------|------|-------------|
| `deploymentId` | `ID!` |  |

Returns `DeploymentLog!` · [DeploymentLog](/api/objects#deploymentlog).

### deploymentStatus

| Argument | Type | Description |
|----------|------|-------------|
| `deploymentId` | `ID!` |  |

Returns `DeploymentStatusUpdate!` · [DeploymentStatusUpdate](/api/objects#deploymentstatusupdate).


† Some provider-specific fields or arguments are omitted from this page. Introspection on the endpoint returns the complete live schema.
