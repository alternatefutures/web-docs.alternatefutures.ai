---
title: "Queries"
description: "Every query the Alternate Clouds GraphQL API exposes, with arguments and return types, generated from the API source."
---

{/* AUTO-GENERATED by scripts/generate-graphql-docs.mjs from @alternatefutures/backend@0.1.2. Do not edit by hand. */}

<Callout type="info">
Generated from the API server source (@alternatefutures/backend 0.1.2) on every merge. Endpoint
`https://api.alternatefutures.ai/graphql`, header `Authorization: Bearer <personal access token>`.
See the [GraphQL API overview](/api) for how to call it. Left out on purpose: provider-specific types, fields and arguments; operations of the retired hosting product (sites, IPFS storage, functions) and admin-only operations.
</Callout>

## Other

### identities

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `subjectType` | `IdentitySubjectType` · [IdentitySubjectType](/api/enums#identitysubjecttype) |  |
| `status` | `IdentityLifecycleStatus` · [IdentityLifecycleStatus](/api/enums#identitylifecyclestatus) |  |

Returns `[IdentityPrincipal!]!` · [IdentityPrincipal](/api/objects#identityprincipal).

### identity

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `did` | `String!` |  |

Returns `IdentityPrincipal` · [IdentityPrincipal](/api/objects#identityprincipal).

### identityCards

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `subjectDid` | `String` |  |
| `status` | `CredentialLifecycleStatus` · [CredentialLifecycleStatus](/api/enums#credentiallifecyclestatus) |  |

Returns `[IdentityCredential!]!` · [IdentityCredential](/api/objects#identitycredential).

### identityVerificationPolicy

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `subjectType` | `IdentitySubjectType!` · [IdentitySubjectType](/api/enums#identitysubjecttype) |  |

Returns `IdentityVerificationPolicy` · [IdentityVerificationPolicy](/api/objects#identityverificationpolicy).

### proofProviders

Returns `[ProofProvider!]!` · [ProofProvider](/api/objects#proofprovider).

### agentIamBinding

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `agentKey` | `String!` |  |

Returns `A2aAgentBinding` · [A2aAgentBinding](/api/objects#a2aagentbinding).

## Swarm runtime (distinct from the agent chat resource below)

### swarmRun

| Argument | Type | Description |
|----------|------|-------------|
| `id` | `ID!` |  |

Returns `SwarmRuntimeRun!` · [SwarmRuntimeRun](/api/objects#swarmruntimerun).

### swarmRuns

Runs of a project, newest first; serviceId narrows to the runs of one runtime service (the web Runs tab).

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `limit` | `Int` |  |
| `serviceId` | `ID` |  |

Returns `[SwarmRuntimeRun!]!` · [SwarmRuntimeRun](/api/objects#swarmruntimerun).

### swarmRunEvents

| Argument | Type | Description |
|----------|------|-------------|
| `id` | `ID!` |  |

Returns `[SwarmRuntimeEvent!]!` · [SwarmRuntimeEvent](/api/objects#swarmruntimeevent).

### swarmDeployment

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `swarmId` | `String!` |  |

Returns `SwarmDeployment!` · [SwarmDeployment](/api/objects#swarmdeployment).

### swarmDeployProgress

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `swarmId` | `String!` |  |

Returns `SwarmDeployProgress` · [SwarmDeployProgress](/api/objects#swarmdeployprogress).

### swarmDefinitionVersions

Registered definition versions of one swarm, newest first (metadata only; bundles never leave the API).

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `swarmId` | `String!` |  |
| `limit` | `Int` |  |

Returns `[SwarmDefinitionVersion!]!` · [SwarmDefinitionVersion](/api/objects#swarmdefinitionversion).

### swarmDefinitionSpec

The authored spec of one registered version (the newest when versionId is omitted); see SwarmDefinitionSpec.

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `swarmId` | `String!` |  |
| `versionId` | `ID` |  |

Returns `SwarmDefinitionSpec!` · [SwarmDefinitionSpec](/api/objects#swarmdefinitionspec).

### swarmModels

Returns `[SwarmModel!]!` · [SwarmModel](/api/objects#swarmmodel).

### swarmRoster

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `swarmId` | `String!` |  |

Returns `SwarmRoster!` · [SwarmRoster](/api/objects#swarmroster).

### swarmRoom

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `swarmId` | `String!` |  |

Returns `SwarmRoom!` · [SwarmRoom](/api/objects#swarmroom).

### swarmRoomSecrets

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `swarmId` | `String!` |  |

Returns `SwarmRoomSecrets!` · [SwarmRoomSecrets](/api/objects#swarmroomsecrets).

### swarmDiscord

The swarm's Discord connection (bot identity, channel map, redeploy state); never the token.

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `swarmId` | `String!` |  |

Returns `SwarmDiscordConnection!` · [SwarmDiscordConnection](/api/objects#swarmdiscordconnection).

### swarmDiscordSecrets

Bridge credential only: the bot token the swarm's sidecar logs in with.

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `swarmId` | `String!` |  |

Returns `SwarmDiscordSecrets!` · [SwarmDiscordSecrets](/api/objects#swarmdiscordsecrets).

### swarmDeploymentLogs

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `swarmId` | `String!` |  |
| `tail` | `Int` |  |

Returns `SwarmDeploymentLogs!` · [SwarmDeploymentLogs](/api/objects#swarmdeploymentlogs).

### runtimeWatch

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `swarm` | `String!` |  |

Returns `RuntimeWatchSnapshot!` · [RuntimeWatchSnapshot](/api/objects#runtimewatchsnapshot).

### runtimeTrace

| Argument | Type | Description |
|----------|------|-------------|
| `runId` | `ID!` |  |

Returns `RuntimeTrace!` · [RuntimeTrace](/api/objects#runtimetrace).

### runtimeStateHistory

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `swarmId` | `String` |  |
| `agentId` | `String!` |  |
| `after` | `String` |  |
| `limit` | `Int!` |  |

Returns `RuntimeStateHistory!` · [RuntimeStateHistory](/api/objects#runtimestatehistory).

### runtimeStateAtVersion

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `swarmId` | `String` |  |
| `agentId` | `String!` |  |
| `version` | `String!` |  |

Returns `JSON!`.

### exportRuntimeAgent

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `swarmId` | `String` |  |
| `agentId` | `String!` |  |
| `format` | `RuntimeAgentExportFormat!` · [RuntimeAgentExportFormat](/api/enums#runtimeagentexportformat) |  |

Returns `RuntimeAgentExport!` · [RuntimeAgentExport](/api/objects#runtimeagentexport).

## Version

### version

Returns `Version!` · [Version](/api/objects#version).

## Templates

### templates

| Argument | Type | Description |
|----------|------|-------------|
| `category` | `TemplateCategory` · [TemplateCategory](/api/enums#templatecategory) |  |

Returns `[Template!]!` · [Template](/api/objects#template).

### template

| Argument | Type | Description |
|----------|------|-------------|
| `id` | `ID!` |  |

Returns `Template` · [Template](/api/objects#template).

## User & auth

### me

Returns `User` · [User](/api/objects#user).

### personalAccessTokens

Returns `PersonalAccessTokenList!` · [PersonalAccessTokenList](/api/objects#personalaccesstokenlist).

### apiKeyRateLimit

Returns `ApiKeyRateLimit!` · [ApiKeyRateLimit](/api/objects#apikeyratelimit).

## Projects

### project

| Argument | Type | Description |
|----------|------|-------------|
| `id` | `ID!` |  |

Returns `Project` · [Project](/api/objects#project).

### resolveProject

Resolve an accessible project by exact ID, name, or slug; ambiguous names/slugs fail closed.

| Argument | Type | Description |
|----------|------|-------------|
| `ref` | `String!` |  |

Returns `Project` · [Project](/api/objects#project).

### projects

Returns `ProjectList!` · [ProjectList](/api/objects#projectlist).

## Service registry

### serviceRegistry

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID` |  |

Returns `[Service!]!` · [Service](/api/objects#service).

### service

One service by id (project access required). Use for on-demand fields such as spend.

| Argument | Type | Description |
|----------|------|-------------|
| `id` | `ID!` |  |

Returns `Service` · [Service](/api/objects#service).

## Workspace metrics (aggregated compute, storage, traffic)

### workspaceMetrics

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID` |  |

Returns `WorkspaceMetrics!` · [WorkspaceMetrics](/api/objects#workspacemetrics).

## Unified deployments (all deployment types across all services)

### allDeployments

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID` |  |
| `limit` | `Int` |  |

Returns `[UnifiedDeployment!]!` · [UnifiedDeployment](/api/objects#unifieddeployment).

## System health

### subscriptionHealth

Returns `SubscriptionHealth!` · [SubscriptionHealth](/api/objects#subscriptionhealth).

## Agent chat

### agent

| Argument | Type | Description |
|----------|------|-------------|
| `id` | `ID!` |  |

Returns `Agent` · [Agent](/api/objects#agent).

### agentBySlug

| Argument | Type | Description |
|----------|------|-------------|
| `slug` | `String!` |  |

Returns `Agent` · [Agent](/api/objects#agent).

### agents

Returns `[Agent!]!` · [Agent](/api/objects#agent).

### chat

| Argument | Type | Description |
|----------|------|-------------|
| `id` | `ID!` |  |

Returns `Chat` · [Chat](/api/objects#chat).

### chats

Returns `[Chat!]!` · [Chat](/api/objects#chat).

### messages

| Argument | Type | Description |
|----------|------|-------------|
| `chatId` | `ID!` |  |
| `limit` | `Int` |  |
| `before` | `String` |  |

Returns `[Message!]!` · [Message](/api/objects#message).

## Observability / APM

### traces

| Argument | Type | Description |
|----------|------|-------------|
| `input` | `TraceQueryInput!` · [TraceQueryInput](/api/inputs#tracequeryinput) |  |

Returns `[Trace!]!` · [Trace](/api/objects#trace).

### trace

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `traceId` | `String!` |  |

Returns `Trace` · [Trace](/api/objects#trace).

### metrics

| Argument | Type | Description |
|----------|------|-------------|
| `input` | `MetricQueryInput!` · [MetricQueryInput](/api/inputs#metricqueryinput) |  |

Returns `[MetricSeries!]!` · [MetricSeries](/api/objects#metricseries).

### logs

| Argument | Type | Description |
|----------|------|-------------|
| `input` | `LogQueryInput!` · [LogQueryInput](/api/inputs#logqueryinput) |  |

Returns `[LogEntry!]!` · [LogEntry](/api/objects#logentry).

### services

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `startTime` | `Date!` |  |
| `endTime` | `Date!` |  |

Returns `[ServiceStats!]!` · [ServiceStats](/api/objects#servicestats).

### observabilitySettings

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |

Returns `ObservabilitySettings` · [ObservabilitySettings](/api/objects#observabilitysettings).

### telemetryUsage

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |
| `startDate` | `Date!` |  |
| `endDate` | `Date!` |  |

Returns `TelemetryUsageSummary!` · [TelemetryUsageSummary](/api/objects#telemetryusagesummary).

## Live container health from provider

### deploymentHealth

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |

Returns `DeploymentHealth` · [DeploymentHealth](/api/objects#deploymenthealth).

## Service container logs

### serviceLogs

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |
| `tail` | `Int` |  |
| `service` | `String` |  |

Returns `ServiceLogResult!` · [ServiceLogResult](/api/objects#servicelogresult).

### attestationMeasurements

The active organization's attestation measurement allowlist.

Returns `[AttestationMeasurement!]!` · [AttestationMeasurement](/api/objects#attestationmeasurement).

### serviceAttestedIdentities

Registry identities linked to this service, newest first. status is
the EFFECTIVE status (a revoked measurement retires its identities): the same verdict the public REST lookup serves.

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |

Returns `[AttestedIdentity!]!` · [AttestedIdentity](/api/objects#attestedidentity).

## Deployment file access (files tab)

### serviceFiles

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |
| `path` | `String!` |  |

Returns `ServiceFileList!` · [ServiceFileList](/api/objects#servicefilelist).

### serviceFileContent

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |
| `path` | `String!` |  |

Returns `ServiceFileContent!` · [ServiceFileContent](/api/objects#servicefilecontent).

### serviceFileAccessPolicy

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |

Returns `ServiceFileAccessPolicy!` · [ServiceFileAccessPolicy](/api/objects#servicefileaccesspolicy).

## Service links (connections between services)

### serviceLinks

| Argument | Type | Description |
|----------|------|-------------|
| `projectId` | `ID!` |  |

Returns `[ServiceLink!]!` · [ServiceLink](/api/objects#servicelink).

## Org billing runway (how long until funds run out)

### orgBillingRunway

Returns `OrgBillingRunway` · [OrgBillingRunway](/api/objects#orgbillingrunway).

## GitHub deploy

### githubAppEnabled

Returns `Boolean!`.

### githubAppSlug

Returns `String`.

### githubInstallations

| Argument | Type | Description |
|----------|------|-------------|
| `orgId` | `ID` |  |

Returns `[GithubInstallation!]!` · [GithubInstallation](/api/objects#githubinstallation).

### githubInstallationRepos

| Argument | Type | Description |
|----------|------|-------------|
| `installationId` | `ID!` |  |

Returns `[GithubRepoSummary!]!` · [GithubRepoSummary](/api/objects#githubreposummary).

### githubRepoBranches

| Argument | Type | Description |
|----------|------|-------------|
| `installationId` | `ID!` |  |
| `owner` | `String!` |  |
| `repo` | `String!` |  |

Returns `[GithubBranchSummary!]!` · [GithubBranchSummary](/api/objects#githubbranchsummary).

## Builds

### buildJob

Fetch a single BuildJob by id (auth: caller must have access to the parent service's project). Used by the Source-tab build-history UI to lazy-load the logs blob on row expand.

| Argument | Type | Description |
|----------|------|-------------|
| `id` | `ID!` |  |

Returns `BuildJob` · [BuildJob](/api/objects#buildjob).

### serviceBuildJobs

List recent BuildJobs for a service, newest first. Logs blob omitted: fetch it via buildJob(id) on demand. Used by the Source-tab build-history list (polled every 3s while a build is in flight).

| Argument | Type | Description |
|----------|------|-------------|
| `serviceId` | `ID!` |  |
| `limit` | `Int` |  |

Returns `[BuildJob!]!` · [BuildJob](/api/objects#buildjob).

## Regions

### regions

Return curated region buckets with live availability + pricing for the picker. Defaults to the standard compute tier when provider is omitted. The gpuModelHint argument is reserved for future per-model price emphasis;
ignored today.

| Argument | Type | Description |
|----------|------|-------------|
| `gpuModelHint` | `String` |  |

Some arguments are omitted. †

Returns `[Region!]!` · [Region](/api/objects#region).


† Some fields, arguments or return types are omitted from this page: provider-specific ones, or ones that belong to retired or admin-only surfaces.
